# IMPORTANT: Replace values with base64 encoded strings
# echo -n "your-value" | base64
apiVersion: v1
kind: Secret
metadata:
  name: bookstore-secrets
  namespace: bookstore
type: Opaque
stringData:
  DB_USER: "bookstoreDBA"
  DB_PASSWORD: "REPLACE_WITH_DB_PASSWORD"
  STRIPE_SECRET_KEY: "REPLACE_WITH_STRIPE_KEY"
  JWT_KEYSTORE_PASSWORD: "devdcorespass"
  JWT_KEY_PAIR_PASSWORD: "devdcorespass"
