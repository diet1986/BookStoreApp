---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.billingService.name }}
  namespace: {{ .Values.global.namespace }}
  labels:
    app: {{ .Values.billingService.name }}
    {{- include "bookstore.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.billingService.replicas }}
  selector:
    matchLabels:
      app: {{ .Values.billingService.name }}
  template:
    metadata:
      labels:
        app: {{ .Values.billingService.name }}
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
      - name: billing-service
        image: {{ include "bookstore.image" (dict "Values" .Values "image" .Values.billingService.image) }}
        imagePullPolicy: Always
        ports:
        - containerPort: {{ .Values.billingService.port }}
        env:
        {{- include "bookstore.commonEnv" . | nindent 8 }}
        - name: SERVER_PORT
          value: {{ .Values.billingService.port | quote }}
        {{- include "bookstore.readinessProbe" .Values.billingService.port | nindent 8 }}
        {{- include "bookstore.livenessProbe" .Values.billingService.port | nindent 8 }}
        resources:
          {{- toYaml .Values.billingService.resources | nindent 10 }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.billingService.name }}
  namespace: {{ .Values.global.namespace }}
  labels:
    app: {{ .Values.billingService.name }}
    {{- include "bookstore.labels" . | nindent 4 }}
spec:
  selector:
    app: {{ .Values.billingService.name }}
  ports:
  - port: {{ .Values.billingService.port }}
    targetPort: {{ .Values.billingService.port }}
  type: ClusterIP
