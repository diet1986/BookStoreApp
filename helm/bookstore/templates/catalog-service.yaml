---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.catalogService.name }}
  namespace: {{ .Values.global.namespace }}
  labels:
    app: {{ .Values.catalogService.name }}
    {{- include "bookstore.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.catalogService.replicas }}
  selector:
    matchLabels:
      app: {{ .Values.catalogService.name }}
  template:
    metadata:
      labels:
        app: {{ .Values.catalogService.name }}
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
      - name: catalog-service
        image: {{ include "bookstore.image" (dict "Values" .Values "image" .Values.catalogService.image) }}
        imagePullPolicy: Always
        ports:
        - containerPort: {{ .Values.catalogService.port }}
        env:
        {{- include "bookstore.commonEnv" . | nindent 8 }}
        - name: SERVER_PORT
          value: {{ .Values.catalogService.port | quote }}
        {{- include "bookstore.readinessProbe" .Values.catalogService.port | nindent 8 }}
        {{- include "bookstore.livenessProbe" .Values.catalogService.port | nindent 8 }}
        resources:
          {{- toYaml .Values.catalogService.resources | nindent 10 }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.catalogService.name }}
  namespace: {{ .Values.global.namespace }}
  labels:
    app: {{ .Values.catalogService.name }}
    {{- include "bookstore.labels" . | nindent 4 }}
spec:
  selector:
    app: {{ .Values.catalogService.name }}
  ports:
  - port: {{ .Values.catalogService.port }}
    targetPort: {{ .Values.catalogService.port }}
  type: ClusterIP
{{- if .Values.autoscaling.enabled }}
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ .Values.catalogService.name }}-hpa
  namespace: {{ .Values.global.namespace }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ .Values.catalogService.name }}
  minReplicas: {{ .Values.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.autoscaling.maxReplicas }}
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: {{ .Values.autoscaling.targetCPUUtilizationPercentage }}
{{- end }}
